{
  "name": "togomoscow",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run railway:build",
    "start": "npm --prefix backend run railway:start"
  }
}
